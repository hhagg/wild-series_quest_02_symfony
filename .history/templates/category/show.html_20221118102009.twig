{% extends 'base.html.twig' %}

{% block title %}Programs into  #{{ category.name}}{% endblock %}

{% block body %}
    <h1>{{category.name}}</h1>
    {# <div class="media-body">
        <p>{{ category.program.poster }}</p>
        <p>{{ category.program.title }}</p>
        <p>{{ category.program.synopsis }}</p>
    </div>

    <a href="{{ path('category_index') }}">
        Back to categories
    </a> #}
    <a href="{{ path('category_index') }}">
        Back to categories
    </a>
{% endblock %}